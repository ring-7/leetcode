# 128最长连续排序
## 题目
给定一个未排序的整数数组 nums ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。
请你设计并实现时间复杂度为 O(n) 的算法解决此问题。

示例 1：
输入：nums = [100,4,200,1,3,2]
输出：4
解释：最长数字连续序列是 [1, 2, 3, 4]。它的长度为 4。

示例 2：
输入：nums = [0,3,7,2,5,8,4,6,0,1]
输出：9

示例 3：
输入：nums = [1,0,1,2]
输出：3

## 题解(哈希表)
```cpp
class Solution {
public:
    int longestConsecutive(vector<int>& nums) {
        unordered_set<int> num_set;
        for (const int& num : nums) {
            num_set.insert(num);
        }

        int longestStreak = 0;

        for (const int& num : num_set) {
            if (!num_set.count(num - 1)) {
                int currentNum = num;
                int currentStreak = 1;

                while (num_set.count(currentNum + 1)) {
                    currentNum += 1;
                    currentStreak += 1;
                }

                longestStreak = max(longestStreak, currentStreak);
            }
        }

        return longestStreak;           
    }
};
```

## STL
### unordered_set和unordered_map
|对比|`unordered_map`|`unordered_set`|
|---|:---|:---|
|存储内容|键值对|单个元素|
|元素是否重复|Key 不重复|元素不重复|
|是否存储额外信息|可以|不可以|
|典型用途|计数、建立映射|去重、判断是否存在|

### unordered_set 常用函数

|函数|作用|
|---|:---|
|`st.insert(x)`|插入元素 x|
|`st.count(x)`|判断 x 是否存在，返回 0 或 1|
|`st.find(x)`|查找 x，返回迭代器|
|`st.erase(x)`|删除元素 x|
|`st.empty()`|判断集合是否为空|
|`st.clear()`|清空集合|

**注意**
其中，count() 和 find() 是刷算法题时经常使用的两个函数。